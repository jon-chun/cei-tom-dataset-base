# CEI Benchmark DMLR 2026 — Replication Dependencies
# Install: pip install -r requirements.txt

# Required for pipeline
pyyaml>=6.0
python-dotenv>=1.0

# Optional: for figure generation
matplotlib>=3.7
numpy>=1.24
scipy>=1.10
